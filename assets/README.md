# Optional build assets

Place the Windows application icon at `assets_/app.ico` when a final icon is available.

The current packaging flow works without it and falls back to the default PyInstaller icon.
